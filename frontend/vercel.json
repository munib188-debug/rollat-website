{
  "buildCommand": "yarn build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://rollat-website.onrender.com/api/$1" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
